export enum QueryParamKeys {
  NAME = "name",
  SPECIES = "species",
  PAGE = "page",
}

export enum Species {
  ALL_SPECIES = "",
  ALIEN = "alien",
  HUMAN = "human",
}

export enum Status {
  ALIVE = "Alive",
  DEAD = "Dead",
  UNKNOWN = "unknown",
}

export enum Tags {
  CHARACTER = "Character",
}
