export enum QueryParamKeys {
  NAME = "name",
  SPECIES = "species",
  PAGE = "page",
}

export enum Species {
  ALL_SPECIES = "",
  HUMAN = "human",
  ALIEN = "alien",
}

export enum Status {
  ALIVE = "Alive",
  DEAD = "Dead",
  UNKNOWN = "unknown",
}

export enum Tags {
  CHARACTER = "Character",
}
